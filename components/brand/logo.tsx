/**
 * public/icesco-logo.png (1811x868, background matted to transparent) reads
 * as: mark (16–646px), a divider (762–770px), then the ICESCO wordmark +
 * subtitle block (862–1750px) — measured by scanning the original source for
 * non-white columns.
 */
export const LOGO_SRC = '/icesco-logo.png'
export const LOGO_SOURCE_WIDTH = 1811
export const LOGO_SOURCE_HEIGHT = 868
export const LOGO_MARK_WIDTH = 700 // crop boundary, just after the mark, before the divider
