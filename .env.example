DATABASE_URL="postgresql://icesco:icesco_dev@localhost:5434/icesco_knowledge_forum"
JWT_SECRET="change-me-in-production"

# Crisis City — mode atelier (sessions live S1-S4, cf. components/crisis-city/atelier/).
# Optionnelles : sans elles, l'atelier bascule automatiquement sur un relais local
# (localStorage + évènement `storage`) qui fonctionne entre onglets d'un même navigateur pour
# tester en solo, mais pas entre plusieurs téléphones physiques — voir SETUP_ATELIER.md.
NEXT_PUBLIC_SUPABASE_URL=""
NEXT_PUBLIC_SUPABASE_ANON_KEY=""
